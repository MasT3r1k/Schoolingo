import { IbanFormatterDirective } from './iban-formatter.directive';

describe('IbanFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new IbanFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
