export interface MarkConfig {
    max_weight: number,
    min_weight: number,
    mark_display: number,
    mark_ids: number,
    max_points: number,
    min_points: number,
    max_topic_length: number,
    min_topic_length: number,
}