export interface MarkConfig {
    max_weight: number,
    min_weight: number,
    mark_display: string[],
    mark_ids: number[],
    mark_actions: string[],
    max_points: number,
    min_points: number,
    max_topic_length: number,
    min_topic_length: number,
}